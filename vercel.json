{
  "framework": "vite",
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts --no-audit --no-fund",
  "buildCommand": "npm rebuild esbuild && npm run build",
  "outputDirectory": "dist"
}